pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()

        //MPChart
        maven {url "https://jitpack.io"}

    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()

        //MPChart
        maven {url "https://jitpack.io"}

    }
}
rootProject.name = "keibo"
include ':app'
